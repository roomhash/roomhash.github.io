{
  "name": "roomhash",
  "version": "1.0.0",
  "private": true,
  "description": "A room in a link. Pure-static P2P chat via WebTorrent trackers.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "verify": "npm test && npm run build"
  },
  "dependencies": {
    "@trystero-p2p/torrent": "^0.25.3"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
